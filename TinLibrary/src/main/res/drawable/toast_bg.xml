<?xml version="1.0" encoding="utf-8"?>
<!--主要按钮  常规状态-->
<shape xmlns:android="http://schemas.android.com/apk/res/android" >

    <!-- 圆角 -->
    <corners
        android:radius="@dimen/toast_radius"
        /><!-- 设置圆角半径 -->

    <!-- 渐变
    android:startColor和android:endColor分别为起始和结束颜色，
    android:angle是渐变角度，必须为45的整数倍。
    另外渐变默认的模式为android:type="linear"，即线性渐变，
    可以指定渐变为径向渐变，android:type="radial"，
    径向渐变需要指定半径android:gradientRadius="50"。
    angle值对应的位置如图：
             -->
    <gradient
        android:useLevel="true"
        android:angle="45"
        android:type="radial"
        android:centerX="0"
        android:centerY="0"
        android:gradientRadius="90"/>

    <!-- 间隔 -->
    <padding
        android:left="@dimen/toast_bg_paddingLeft_Right"
        android:top="@dimen/toast_bg_paddingTop_Bottom"
        android:right="@dimen/toast_bg_paddingLeft_Right"
        android:bottom="@dimen/toast_bg_paddingTop_Bottom"/><!-- 各方向的间隔 -->

    <!-- 大小 -->
    <!--<size-->
    <!--android:width="50dp"-->
    <!--android:height="50dp"/>&lt;!&ndash; 宽度和高度 &ndash;&gt;-->

    <!-- 填充的颜色 -->
    <solid
        android:color="@color/toast_bg_color"/>



    <!-- 描边 dashWidth：实线宽度   dashGap,虚线宽度-->
    <stroke
        android:width="1px"
        android:color="@color/toast_strock_color"
        android:dashWidth="1px"
        android:dashGap="0px"/>

</shape>